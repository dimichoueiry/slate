# Server-side secrets for Slate's API functions (never bundled to the client).
# In production, set these in your Vercel project: Settings → Environment Variables.
TAVILY_API_KEY=your-tavily-key-here
